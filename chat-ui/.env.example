# Keycloak OIDC Configuration
# Authority URL for Keycloak realm (defaults to localhost if not set)
VITE_KEYCLOAK_AUTHORITY=http://localhost:8080/realms/demo

# Keycloak client ID (defaults to demo-app if not set)
VITE_KEYCLOAK_CLIENT_ID=demo-app
